# Reader Revamp — one-shot `/goal` prompt

Run this with `/goal` (AFK-safe). It restyles ONLY the experience-v2 landing's iframe letter-reader into an era-evoking "letter read by lamplight" design, wraps the build in 3 rounds of simulated expert + user testing, then verifies and deploys to production. Review the recap doc it produces when it finishes.

```
/goal Revamp the experience-v2 landing's letter reader into an era-evoking "letter read by lamplight" design — a luminous warm-parchment DOCUMENT resting in a DARK, warm, lamplit archival FRAME — scoped to the landing's isolated iframe reader ONLY, with ALL current functionality preserved, hardened through 3 rounds of simulated expert + user testing, then deployed to production. Work on the current `outreach-engine` branch; do NOT touch `main`.

=== GROUNDING (verify these against the live code before building; they are the load-bearing seams) ===
- `reader.html` (repo ROOT) — the ISOLATED iframe host the landing opens. It loads `/_design.css` + `/_overlay.css` + the reader stack (`/_design-data.js`, `/_design-ui.js`, `/_deeplink.js`, `/_overlay-data.js`, `/_letter-nav.js`, `/_overlay.js`, `/_reader.js`) then calls `HubbellOverlay.showLetter(id)`. Because it is an iframe, ANY CSS it loads affects ONLY this document — so the new theme can override aggressively (even bare element selectors) with zero risk to other pages. This is the ONLY place the new stylesheet is loaded. Its `<head>` already has a `<style>` block with `html,body{margin:0;background:transparent}`.
- `_reader.js` (root, ~745 lines) — renders the reader. DO NOT change its behavior; you MAY add small, additive, backward-compatible structural hooks if a grouping wrapper genuinely helps the layout, but prefer pure CSS. Real class hooks to style (confirm by reading the render assembly ~lines 650–674 and the section builders): `reader-header` (border-bottom = brother color), `reader-correspondence` + `reader-pill`/`reader-pill-label` (From→To), `reader-flags-inline`/`reader-flag`, `reader-meta` (`.rm-date`/`.rm-loc`/`.rm-id`) + `reader-map-link`, `reader-context-strip` (~line 432), `reader-exec-summary` (uses `--exec-c` = brother color, ~line 655), `reader-health`/`-status`/`-conf`/`-conf-desc`/`-symptoms`/`-symptom` (~134–161), `reader-people`/`reader-places` (`<h4>` + `reader-tags`/`reader-tag`, ~584–594), `reader-body`/`reader-para` (transcription; supports `.hide-<flag>` collapses + search `<mark>`), and the fixed action/nav bar `reader-nav-bar`/`reader-nav-arrow`/`reader-nav-mode`/`reader-nav-mode-btn` (~208–214, the `Author's Letters` / `All Letters` mode filter) + `reader-close`/share/back from `_overlay.js` `headerActions()` (`hubbell-overlay-close`/`-back`, `dl-modal-share`). The panel container is `.hubbell-overlay-panel` (right-side slide-in).
- `_overlay.css` + `_design.css` (root) — the CURRENT styling: a bright near-white panel (`--bg: oklch(98.2% 0.008 85)`), dark ink, Source Serif + Inter + JetBrains Mono, warm-orange accent (`--accent` h=24). This is the "cold / generic / disjointed" look to replace. **DO NOT EDIT these two files** (they are shared by the dashboard, the-collection, viz-people-web, search). All retoning happens in the new file, inside the iframe scope.
- `experience-v2/landing.html` — `openReader(id)` sets `#rdrframe.src = '/reader?id='+id+'&rv=2'` then opens the `.rdrwrap` overlay; the four `.read-letter` pills call it. `&rv=N` is a CACHE-BUST (reader.html has `Cache-Control: max-age=3600`): you MUST bump `rv` (to `rv=3`, then higher on each subsequent change) in `openReader` every time you change `reader.html` or the new stylesheet, or browsers serve the stale version. The landing identity tokens to evoke live in landing.html's `<style>` `:root`: `--paper:#ece3d2; --lantern:#e0b070; --ink:#16130d; --muted:#9aa3ab` and brothers `--b-alex:#B8860B; --b-henry:#2D5F8A; --b-james:#4A7C59; --b-charles:#8B3A3A`; textures = the grain SVG data-URI, vignette, and lamplight radial glows already in landing.html (copy/adapt these into the reader theme).
- `.vercelignore` is a default-deny allowlist that serves root `_*.css`/`_*.js` and top-level `*.html` — so a new root `_reader-era.css` is served automatically; no ignore changes needed. `vercel.json` has `cleanUrls:true` (so `/reader` serves `reader.html`).
- Data: `03-data/all-letters.json` (letter records: `id`, `author`, `people[]`, `places[]`, `loc`, `ss` summary, boolean flags `hasBattle`/`hasIllness`/`hasWound`/`hasDeath`, health fields). Known ids: voices = `LTR-1863-09-03-001` (Alexander), `LTR-1861-07-21-001` (Henry), `LTR-1864-10-04-001` (James), `LTR-1864-03-06-001` (Charles); the keystone 1996 document = `DOC-INTRO-1996-10-28-001` (opens via the-collection's `opts.asDocument` path — confirm it renders in `reader.html`; if the doc isn't in `_overlay-data.js`'s `LETTER_INDEX`, note it and test it via the-collection instead).
- Deploy mechanism (see memory `reference_deploy_mechanism`): production = CLI `vercel --prod --yes` (run BACKGROUNDED, ~2.4 GB upload, long timeout); a git push only makes a PREVIEW. `git status` should be clean on `outreach-engine`; base all work there.
- Honor the project's voice/provenance ethos: never fabricate or alter letter text/dates/data; this is genealogical/archival work where accuracy and provenance are paramount (memory `user_hubbell_researcher`, `feedback_identity_matching`).

=== DECIDED (do not re-litigate) ===
1. SCOPE = landing-reader-ONLY pilot. Style only what `reader.html` loads, via ONE new root file `_reader-era.css` linked in `reader.html` AFTER `/_overlay.css`. Do NOT modify `_overlay.css`, `_design.css`, or `_reader.js` behavior. The dashboard/collection/people-web/search readers stay exactly as they are.
2. SURFACE DIRECTION = "a letter read by lamplight": a luminous warm-PARCHMENT document (the `reader-body` transcription is the hero) set inside a DARK, warm, lamplit archival FRAME (header, dateline, synopsis, mentions, footer rail = the "archivist's desk/folder"). Two materials, clear hierarchy.
3. PRESERVE 100% of function: mode filter (Author's/All Letters), prev/next nav, share, close, View on Map link, exec-summary synopsis, people/places clickthrough tags, flags, health block, in-text people/place marks, search highlight. Visual change only.
4. Palette = the landing's `--paper`/`--lantern`/`--ink` + brother colors as restrained "wax-seal/spine" markers (NOT loud fills). Type = Source Serif for the manuscript body (generous measure ~60–64ch, manuscript leading ~1.7, paragraph indents, a tasteful drop-initial on the first paragraph), JetBrains Mono for labels/meta as an "archival catalog" voice, Inter sparingly.
5. Motion = quiet and restrained (folder-open ease-in, a faint parchment lift, soft hover states, one gentle settle on the brother seal). No bounce, no noise. Respect `prefers-reduced-motion`.
6. The production deploy at the END is pre-authorized by the user for this run (it is the explicit goal). It is gated behind full preview + Playwright verification first.

=== SCOPE & SEQUENCE (finish + verify each before the next) ===
A. AUDIT & SCAFFOLD. Read `_reader.js` render + `_overlay.css`/`_design.css` reader rules; record which visual props are token-driven (`var(--bg/--ink/--accent/--rule…)`) vs hardcoded — this tells you whether retoning `:root` cascades or whether each element needs an explicit override. Create `_reader-era.css` (root), link it in `reader.html` after `/_overlay.css`, set `reader.html` body to the dark frame, bump landing `openReader` to `rv=3`. Get a baseline Playwright screenshot of the current reader for before/after.
B. BUILD ERA READER v1 (the full first pass): (1) the dark lamplit FRAME (panel chrome, vignette + grain texture, lamplight glow); (2) the PARCHMENT document body — warm cream surface, faint fiber grain, deckled/aged edge, Source Serif manuscript type, drop-initial, ~62ch measure, indents, search-`<mark>` styled as a soft highlight; (3) the LETTERHEAD — From→To with a small brother-colored wax-seal dot, flags as quiet engraved icon-tags; (4) the DATELINE + catalog id (`reader-meta` → "Harrisonburg · Oct 4, 1864" + small archival ref); (5) the ARCHIVIST'S-ABSTRACT synopsis (`reader-exec-summary` set-apart italic serif under a hairline gold rule with a marginal "summary" label); (6) the "condition note" HEALTH card (keep status/confidence/symptoms data, retone from clinical badges); (7) the foot "MENTIONED HERE" apparatus (`reader-people`/`reader-places` → tidy gold-outline clickable tags, scholarly-edition feel, clickthroughs intact); (8) the FOOTER RAIL — segmented `This brother / All letters` mode toggle, refined prev/next, inline "see on the map"; (9) quiet motion. IN SCOPE: only the landing reader's appearance. OUT OF SCOPE (name as the NEXT step): applying this theme globally to the dashboard/collection readers, and the broader per-section-page redesign.
C. ROUND 1 — TEST & REVISE (see TESTING LOOP below). Render the representative letter set, run the expert trio + 3 user profiles, synthesize feedback, make concrete revisions, re-render to confirm. Log Round-1 findings + changes.
D. ROUND 2 — TEST & REVISE. Same loop on the revised reader; push on whatever Round 1 left weak; revise.
E. ROUND 3 — TEST & REVISE. Same loop; final substantive improvements; revise.
F. FINAL POLISH + FULL VERIFICATION. Responsive/mobile pass; `prefers-reduced-motion`; contrast/legibility check on the parchment; FUNCTIONAL regression via Playwright on a real deployment — every mode toggle, every clickthrough (person/place tags, map link, prev/next, share), scroll, and the open→close→reclick cycle (`pointer-events` MUST return to `none` after close; bump `rv` so the test hits fresh assets). THEN deploy: `vercel --prod --yes` (backgrounded, long timeout). Verify LIVE on `hubbell-civil-war-history.vercel.app`: reader opens/closes/reclicks cleanly with the new look, `/PHASE-3-PLAN.md` still 404 (internal-files gate intact), site pages still 200.
G. RECAP DELIVERABLES (mandatory; see DELIVERABLES).

=== TESTING LOOP (apply identically in Rounds 1, 2, 3 — this is the mandatory wrapper) ===
1. RENDER the current reader with Playwright across a REPRESENTATIVE letter set (screenshot each, full where possible): one with a HEALTH/wound block (query `all-letters.json` for `hasWound`/`hasIllness` true — James's wounding letters are candidates), one with MANY people/places (max `people[]`/`places[]`), a MOTHER letter (`author:"mother"`), a SHORT letter, plus the keystone 1996 document. Use `/reader?id=<ID>&rv=<current>` (bump rv as you change files).
2. EXPERT TRIO critique — role-play three experts giving POINTED, specific, non-sycophantic critique of the rendered screenshots (look, feel, hierarchy, era-fidelity, usability), each ending with their top strengths + top weaknesses + concrete fixes:
   - a UI/visual designer (composition, type, color, texture, craft, the "disjointed elements" problem),
   - a UX specialist (clarity, hierarchy, navigation/filters, clickthrough affordances, mobile, accessibility/contrast),
   - a Civil-War / archival / historical-data expert (does it honor the era and the document's gravity without kitsch; is the data/provenance legible and dignified).
3. SAMPLE USER TESTING — role-play three distinct likely users reacting to how it LOOKS and FEELS and what's strong/weak for them:
   - a genealogy researcher / family descendant (wants accuracy, readability, the people/places, "is this MY ancestor"),
   - a casual history enthusiast (wants atmosphere, easy reading, not overwhelmed),
   - a museum educator / academic (wants credibility, citation/reference clarity, shareability).
   Keep personas grounded and critical — surface REAL weaknesses, not praise.
4. SYNTHESIZE the round's feedback into a short ranked list of concrete changes (separate must-fix from nice-to-have). MAKE the changes. RE-RENDER to confirm they landed and didn't regress function. Record "Round N: what they said → what I changed."
Each round must produce genuinely DIFFERENT, improving feedback — do not repeat the same notes; escalate scrutiny (Round 1 = big-picture look/feel; Round 2 = hierarchy/flow/edge-cases; Round 3 = craft/polish/details). If a round surfaces a functional regression, fixing it is must-fix.

=== PER-ITEM RHYTHM (apply to every build item A,B,F) ===
1. Build the core.
2. Test concretely: render the reader in a browser (Playwright) on a deployed PREVIEW (push → preview; CSS/HTML only, fast) or the local-equivalent; screenshot; confirm no console errors beyond the known harmless `/favicon.ico` 404; confirm the shared dashboard/collection readers are UNAFFECTED (spot-check one — its look must not change).
3. 3 loops of self-critique: each loop = critique once as a senior front-end engineer (scope isolation — nothing leaked to the global readers; clean overrides; no `_reader.js` behavior change; perf; reduced-motion) and once as a simulated end-user (is it beautiful, era-true, legible, on-brand; is anything confusing). Iterate.
4. Capture for the recap.

=== GUARDRAILS / USER-GATED — flag and pause, do not push past ===
- Do NOT edit `_overlay.css`, `_design.css`, or change `_reader.js` BEHAVIOR — the shared readers (dashboard/collection/people-web/search) must look and work EXACTLY as before. After any change, spot-verify one shared reader is unchanged; if you cannot keep changes isolated to `reader.html`'s scope, STOP and report.
- Do NOT alter any letter text, date, name, place, summary, or data value — visual/CSS only. Never fabricate content.
- Do NOT touch `main`. Do NOT modify the `.vercelignore` gate or expose any internal file; after deploy, re-confirm `/PHASE-3-PLAN.md` and a `04-analysis/*` path return 404.
- The single outward action is the FINAL `vercel --prod` deploy (pre-authorized for this run) — but ONLY after preview + full Playwright functional verification pass. If verification fails, do NOT deploy; report what's broken.
- If the 1996 document or any representative letter can't be rendered in `reader.html`, note it and proceed with the others rather than guessing.

=== OPEN VALUES (propose sensible defaults; mark user-tunable) ===
- Parchment warmth/contrast level, drop-initial style, exact body measure (default ~62ch), texture intensity (default subtle), and motion durations — pick tasteful defaults, expose them as CSS custom properties at the top of `_reader-era.css` with comments so the user can tune later.
- Whether the dark frame is near-black-warm or deep-sepia — default deep warm charcoal that matches the landing's `#0a0b0e`/lamplight; mark tunable.
- Final `rv` value — whatever increment leaves landing `openReader` and `reader.html` consistent and cache-fresh.

=== DELIVERABLES ===
1. The implemented revamp on `outreach-engine` (committed + pushed): new `_reader-era.css`, the `<link>` + any minimal scope-safe tweaks in `reader.html`, the `rv` bump in `experience-v2/landing.html`. Deployed to production and verified live.
2. Before/after Playwright screenshots of the reader (and of one shared reader proving it's unchanged), saved under the session/output dir.
3. EDITORIAL RECAP doc at repo root `READER-REVAMP-RECAP.md` (or `tasks/` if that's the project's docs home) — MANDATORY, layman-termed, well-referenced (link the real files/paths touched). Per stage (A,B, each test Round, F): what was built/decided, the approach + why, key decisions/tradeoffs/learnings, where & how to test it (the exact `/reader?id=…&rv=…` URLs), and exactly what the user should review or might give feedback on. Include a compact "what each of the 3 rounds changed" table.
4. KEY CONSIDERATIONS (section in the recap) — MANDATORY: watch-outs/risks/opportunities/tradeoffs to weigh (e.g., the divergence between this landing reader and the still-generic shared readers; cache/`rv` discipline; any accessibility/contrast tradeoff of parchment; whether to globalize next).
5. SUGGESTED NEXT STEPS (section in the recap) — MANDATORY, prioritized, seeding the NEXT `/goal-prompt`: e.g., (a) promote this theme to the shared readers (dashboard/collection) once approved; (b) the broader per-section-page immersive redesign; name dependencies (founder approval of the look first).

=== DONE WHEN ===
- `_reader-era.css` exists and is loaded ONLY by `reader.html`; the landing reader renders the era-evoking "lamplight parchment" design across all representative letters; the shared dashboard/collection readers are visually UNCHANGED (verified).
- All current reader functions verified working via Playwright on the live site: mode toggle, prev/next, share, close, map link, people/place clickthroughs, in-text marks, search highlight, and open→close→reclick (`pointer-events` releases to `none`).
- 3 full test rounds ran, each with the expert trio + 3 user profiles, each producing distinct improving feedback and concrete revisions; the "what each round changed" log exists.
- Deployed to production via `vercel --prod`; live verification passed (new look live; internal-files gate still 404; site pages 200; reader cycle clean with bumped `rv`).
- All three mandatory recap deliverables written. Everything committed + pushed on `outreach-engine`; `main` untouched.
```
